export default function AttendanceFilterBar({ search, onSearch, dateFrom, onDateFrom, dateTo, onDateTo, department, onDepartment, departments, status, onStatus, onClear }) {
  const control = "h-10 rounded-md border border-slate-300 bg-white px-3 text-sm text-slate-700 outline-none focus:border-slate-500 focus:ring-2 focus:ring-slate-200";
  return <div className="grid gap-2 border-b border-slate-200 bg-slate-50/70 p-3 sm:grid-cols-2 sm:px-5 lg:flex lg:flex-wrap lg:items-center"><label className="min-w-0 lg:min-w-64 lg:max-w-md lg:flex-1"><span className="sr-only">Search employee</span><input type="search" value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search employee" className={`${control} w-full`} /></label><label><span className="sr-only">From date</span><input type="date" value={dateFrom} onChange={(event) => onDateFrom(event.target.value)} className={`${control} w-full lg:w-40`} title="From date" /></label><label><span className="sr-only">To date</span><input type="date" value={dateTo} onChange={(event) => onDateTo(event.target.value)} className={`${control} w-full lg:w-40`} title="To date" /></label><label><span className="sr-only">Department</span><select value={department} onChange={(event) => onDepartment(event.target.value)} className={`${control} w-full lg:w-44`}><option value="">All departments</option>{departments.map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="sr-only">Status</span><select value={status} onChange={(event) => onStatus(event.target.value)} className={`${control} w-full lg:w-44`}><option value="">All statuses</option><option>Present</option><option>Late</option><option>Absent</option><option>Missing Checkout</option><option>On Leave</option></select></label><button type="button" onClick={onClear} disabled={!search && !dateFrom && !dateTo && !department && !status} className="h-10 px-3 text-left text-sm font-medium text-slate-600 hover:text-slate-950 disabled:cursor-not-allowed disabled:opacity-40 lg:text-center">Clear filters</button></div>;
}
