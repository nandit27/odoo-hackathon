import { Link } from "react-router-dom";
import ScheduleStatusBadge from "./ScheduleStatusBadge.jsx";

export default function TimeOffTypeTable({ types }) {
  const headings = ["Type name", "Code", "Paid / unpaid", "Approval required", "Status", "Actions"];
  return <div className="overflow-x-auto"><table className="w-full min-w-[760px] divide-y divide-slate-200 text-left text-sm md:min-w-0"><thead className="bg-slate-50 text-[11px] font-semibold uppercase tracking-wider text-slate-500"><tr>{headings.map((heading) => <th key={heading} scope="col" className="whitespace-nowrap px-4 py-3 first:pl-5 last:pr-5">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100 bg-white">{types.map((type) => <tr key={type.id} className="transition-colors hover:bg-slate-50"><td className="px-4 py-3 pl-5"><p className="font-semibold text-slate-900">{type.name}</p><p className="max-w-xl truncate text-xs text-slate-500">{type.description}</p></td><td className="whitespace-nowrap px-4 py-3 font-mono text-xs font-semibold text-slate-700">{type.code}</td><td className="whitespace-nowrap px-4 py-3"><span className={`inline-flex rounded-full px-2 py-0.5 text-xs font-medium ring-1 ring-inset ${type.paid ? "bg-emerald-50 text-emerald-700 ring-emerald-600/20" : "bg-slate-100 text-slate-600 ring-slate-500/20"}`}>{type.paid ? "Paid" : "Unpaid"}</span></td><td className="whitespace-nowrap px-4 py-3 text-slate-700">{type.approvalRequired ? "Yes" : "No"}</td><td className="whitespace-nowrap px-4 py-3"><ScheduleStatusBadge status={type.status} /></td><td className="whitespace-nowrap px-4 py-3 pr-5"><Link to={`/time-off-types/${type.id}/edit`} className="font-semibold text-blue-700 hover:underline">Edit</Link></td></tr>)}</tbody></table></div>;
}
