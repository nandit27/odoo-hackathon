export default function TimeOffTypeFilterBar({ search, onSearch, status, onStatus, onClear }) {
  const control = "h-10 rounded-md border border-slate-300 bg-white px-3 text-sm text-slate-700 outline-none focus:border-slate-500 focus:ring-2 focus:ring-slate-200";
  return <div className="flex flex-col gap-2 border-b border-slate-200 bg-slate-50/70 p-3 sm:flex-row sm:items-center sm:px-5"><label className="min-w-0 flex-1 sm:max-w-xl"><span className="sr-only">Search time off types</span><input type="search" value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search type name or code" className={`${control} w-full`} /></label><label><span className="sr-only">Filter by status</span><select value={status} onChange={(event) => onStatus(event.target.value)} className={`${control} w-full sm:w-40`}><option value="">All statuses</option><option>Active</option><option>Inactive</option></select></label><button type="button" onClick={onClear} disabled={!search && !status} className="h-10 px-3 text-left text-sm font-medium text-slate-600 hover:text-slate-950 disabled:cursor-not-allowed disabled:opacity-40 sm:text-center">Clear filters</button></div>;
}
